# =============================================================
# VERCEL ENVIRONMENT VARIABLES  (Frontend — TanStack Start)
# Copy each of these into: Vercel → Project → Settings → Environment Variables
# =============================================================

# ── Supabase ──────────────────────────────────────────────────
# Found in: Supabase Dashboard → Settings → Data API
VITE_SUPABASE_URL=https://fzyqalvvbbndyjtxcmhf.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_dWCKcilRumjcvD7MHJhwjw_0Amy0NW4
VITE_SUPABASE_PROJECT_ID=fzyqalvvbbndyjtxcmhf

# Also needed for SSR (Nitro server-side rendering on Vercel)
SUPABASE_URL=https://fzyqalvvbbndyjtxcmhf.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_dWCKcilRumjcvD7MHJhwjw_0Amy0NW4

# Supabase Service Role key — used ONLY on the server (SSR), never exposed to the browser
# Found in: Supabase Dashboard → Settings → Data API → service_role key
SUPABASE_SERVICE_ROLE_KEY=FILL_IN_FROM_SUPABASE_DASHBOARD

# ── Pesaki Backend (Render) ───────────────────────────────────
# The public URL of your Render pesaki-server service
# e.g. https://pesaki-server.onrender.com
VITE_PESAKI_API_URL=https://pesaki-server.onrender.com
VITE_WEBSOCKET_URL=https://pesaki-server.onrender.com

# ── Database (for migrations only — not needed at runtime) ────
# Found in: Supabase Dashboard → Settings → Database → Connection string (URI)
DATABASE_URL=FILL_IN_FROM_SUPABASE_DASHBOARD
