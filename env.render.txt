# =============================================================
# RENDER ENVIRONMENT VARIABLES  (Backend — pesaki-server / Fastify)
# Copy each of these into: Render → Service → Environment tab
# =============================================================

# ── Supabase ──────────────────────────────────────────────────
# Found in: Supabase Dashboard → Settings → Data API
SUPABASE_URL=https://fzyqalvvbbndyjtxcmhf.supabase.co

# Service Role key (admin — keep this secret, backend only)
# Found in: Supabase Dashboard → Settings → Data API → service_role key
SUPABASE_SERVICE_ROLE_KEY=FILL_IN_FROM_SUPABASE_DASHBOARD

# ── Upstash Redis ─────────────────────────────────────────────
# Found in: https://console.upstash.com → your Redis database → REST API
UPSTASH_REDIS_URL=FILL_IN_FROM_UPSTASH_DASHBOARD
UPSTASH_REDIS_TOKEN=FILL_IN_FROM_UPSTASH_DASHBOARD

# ── Server Config ─────────────────────────────────────────────
PORT=4000
NODE_ENV=production

# CORS: set this to your Vercel frontend URL
# e.g. https://pesakicoke.vercel.app  or  https://pesaki.co.ke
CORS_ORIGIN=FILL_IN_YOUR_VERCEL_FRONTEND_URL

# ── M-Pesa / Safaricom Daraja ─────────────────────────────────
# Found in: https://developer.safaricom.co.ke → your app
MPESA_CONSUMER_KEY=FILL_IN_FROM_DARAJA_PORTAL
MPESA_CONSUMER_SECRET=FILL_IN_FROM_DARAJA_PORTAL
MPESA_SHORTCODE=FILL_IN_FROM_DARAJA_PORTAL
MPESA_PASSKEY=FILL_IN_FROM_DARAJA_PORTAL
MPESA_ENV=sandbox
# Change to your live Render URL once deployed:
MPESA_CALLBACK_URL=https://pesaki-server.onrender.com/mpesa/callback

# ── RapidAPI (NSE / Forex data) ───────────────────────────────
# Found in: https://rapidapi.com → your app subscription
RAPIDAPI_KEY=FILL_IN_FROM_RAPIDAPI
RAPIDAPI_HOST=FILL_IN_FROM_RAPIDAPI
